# Lifecycle UI release — implementation status

Task t_20ee7543; base 58cae34; worktree only; no merge/push/PR/deployment.

## Blocking scope divergence — resolved

Lifecycle is the ONE authoritative results path. No legacy second results block.

- `RentenlueckeCalculator.tsx`: removed `SummaryCards`/`ScenarioOutcomePanel`/`YearlyTable`/`InsuranceBreakdown` imports and legacy `#ergebnis` block. Single `#ergebnis` renders only `LifecycleResults` + `LifecycleChart` + `LifecycleYearlyTable` from the common ledger.
- `useScenarioState.ts`: removed live legacy calc (`simulateHistoricalBootstrapReferenceScenario`/`runHistoricalBootstrapSimulation`). Keeps `result=null`/`stochasticSummary=null` constants for compat. `insuranceIssues` uses `insuranceSetupIssuesWithoutEstimator`; `isValid`/`lifecycleValid` no longer gated by pooled estimator. `runLifecycleBootstrap` uses new `{input,streams,portfolioBuckets,classification,historicalSettings,tax,firstCalendarYear}` signature with per-path full years.
- `retirementInsurance.ts`: added `insuranceSetupIssuesWithoutEstimator` (duplicate without `estimatorSetupIssues`). Preserves manual KV/PV + manual capital assessment UI.
- `RetirementInsuranceSection.tsx`: removed `CapitalEstimatorSetup` + `needsEstimator` gate. Keeps `capitalMode` selector + manual inputs + return-source controls.
- `LifecycleChart.tsx`: rewritten to common-ledger real (nominal/F) reference + bootstrap P10/P50/P90 + survival (`getSurvivalProbabilityForAgeEnd`) + blocked handling. No legacy charts.
- `LifecycleResults.tsx`: label `Benötigtes Startkapital heute (Eröffnung, skaliert heutige Bestände)` matches searched start/time. Unsupported stays explicit (`Feste Reserven, nicht monotone Inflation oder Nullstart ohne Beweis bleiben explizit unsupported; kein Bypass`). No proof flags.
- `LifecycleYearlyTable.tsx`: yearly rows from `result.reports` (same simulation): contribution/withdrawal covered/uncovered, current/arrears/remaining tax, KV/PV paid/unfunded, closing/anchor. No fresh allowance/year at liquidation.
- UI tests converted to new required setup with same behavioral guarantees (not old live results to pass tests): `RentenlueckeCalculator.test.tsx` single-path headings + yearly columns + required-capital label; `useScenarioState.test.ts` incomplete-check via `lifecycleRun`/`lifecycleValid` and contribution/liquidation increase on `amountMonthlyToday`; `insurancePersistence.test.ts` persistence/reset guarantees via `lifecycleRun`/`lifecycleValid` with full lifecycle setup (classification/basis/taxCash/milestones).

## Coordinator financial concerns — resolved with regressions

- Fail-closed liquidation (`lifecycleScenario.summarizeLedgerResult`): removed try/catch substituting gross closing wealth. `liquidateLifecycle` errors propagate; no plausible fake net result. Regression: `lifecycleScenario.test.ts` fails closed on terminal liquidation (terminalInflation 0/-1 throws).
- No duplicate liability (`summarizeLedgerResult`): `liquidationOutstandingLiability = outstandingLiability` only (was `+ remaining`). Contract verified: `investmentTax.terminal.valueHypotheticalLiquidation` nominal is already net (`totalValue - outstandingLiability`) and outstanding already includes prior unpaid (`unpaidTax` after terminal `reconcileTax`); `remainingLiabilitiesTotal` is reported separately from yearly reports, never subtracted again. Regression: outstanding equals direct `liquidateLifecycle` outstanding; `liquidationNominal == max(0, nominal - incrementalInsurance)`; `liquidationReal == nominal/F`.
- Per-path inflation rebuild (`runLifecycleBootstrap`): rebuilds full `LedgerYearInput[]` per path via `buildLedgerYears` (contributions/spending/allowance/manual insurance/pension-rental indexed by path F), passes `{years, fullYears}`; `lifecycleLedger.adapters.runLedgerBootstrap` prefers `fullYears` (validates age/year match + coverage + positive F), fallback to market-override for old tests. Regression: `buildLedgerYears` with F=1 vs F=2 doubles allowance/withdrawal/contribution/manual KV/PV; bootstrap trajectory test checks `yearlyClosingNominal`/`yearlyAnchorNominal`/`yearlyInflationFactors` per completed path.
- Costs exactly once: `resolveComponentNominalReturn` returns raw nominal (historical: no cost; synthetic: sampled without cost) and `deriveMarketYears` wraps once with `applySourceCostTreatment` — same as `generateHistoricalReturnPath`; `resolveComponentExpectedNominalReturn`/`expectedBucketReturns`/`deriveDeterministicMarketYears` apply once via `applyCostTreatment`. Verified against `returnSeriesRegistry` treatments (`deductBucketAnnualCost` for JST/synthetic, `netOfFundCosts` for bundled ETFs). Regression: ETF 2012 raw unchanged with 25% bucket cost; JST bills 1950 nominal minus exactly 1% cost in both expected and sampled paths.
- Deposit negatives honest, no clamp: both market builders throw `Deposit nominal ... nonnegative rates only` (deposit ledger `completeKeys` requires nonnegative rates; `applyAnnualPricesAndInterest` negative interest would fail). Cash range verified: JST bills real min -7.7% (1951); bills nominal with German CPI min -6.5% (1950); with fixed 2% inflation 1951 nominal about -5.9%; synthetic cash mean 2% vol 1% can sample negative. Bootstrap hitting a negative aborts with explicit `Bootstrap unvollständig: Deposit nominal ...` (reference stays visible); no silent clamp or invalid default forecast. Regressions: both builders throw; bootstrap trajectory test accepts explicit throw as honest handling.
- Re-prefill preserves intent (`lifecycleDraft.rePrefillLifecycleMilestones`): only index 0 refilled from holdings; later milestones untouched. Regression: `lifecycleDraft.test.ts` checks index 1 `fixedReserve` preserved after re-prefill.
- Plots/yearly rows on activated ledger, no pooled-estimator gating: charts/tables read `lifecycleRun.result.reports` + bootstrap path trajectories from the same engine; acquisition basis required explicitly per fund (`basisIssues`, zero distinct from missing); no automatic bond-fund exclusion; unsupported holdings get no partial automatic coverage (explicit lifecycle issue). Covered by single-path UI tests + `basisIssues` tests.
- F convention + horizon identity: `cumulativeInflationFactors` builds year-end cumulative F (`F[i]=(1+r0)*...*(1+ri)`); `buildLedgerYears` uses market F for nominal inputs; terminal deflator is last-year F (`years.at(-1).inflationFactor`); `liquidateLifecycle`/`valueHypotheticalLiquidation` deflates by same F (`real=nominal/F`, deflation F<1 allowed, F<=0 throws). Regression: 40/50/60-year no-tax identity (cash-only, zero flows, huge allowance, basis 0, deposit 0%) keeps `closingNominal==liquidationNominal==50000`, `real==nominal/F`, zero remaining/outstanding with nonzero inflation.
- Required capital semantics: searches opening capital today (scales heutige Bestände via `totalWealthForCapital`, preserves positive total, scales acquisitionCost proportionally), horizon/state from base years, terminal F from last year. Fixed-reserve/nonmonotone/zero-start return explicit `unsupported` (no bypass flags); bounding failure throws `RequiredCapitalCalculationError` mapped to `nonconverged`. Regression: converged/unsupported statuses; reserve case unsupported.

Official basis rate: BMF 13 Jan 2026, 3.20%. Default `0.032` (`LIFECYCLE_BASIS_DEFAULT` from `DEFAULT_PROJECTED_BASIS_RATE`) and advanced-editable in `LifecycleTaxSetup`; kept. No other rate changes.

## Test receipts (final, 2026-09-17)

- `GOMAXPROCS=2 npx vitest run --maxWorkers=1 src/features/rentenluecke/model/lifecycleScenario.test.ts src/features/rentenluecke/model/lifecycleDraft.test.ts`: 21 passed (11 pre-existing + 10 new: fail-closed, outstanding-once, per-path rebuild, cost-once, deposit-throws x2 paths, 40/50/60 identity, search explicit, bootstrap trajectories).
- `GOMAXPROCS=2 npx vitest run --maxWorkers=1 src/features/rentenluecke/hooks/__tests__/useScenarioState.test.ts src/features/rentenluecke/hooks/__tests__/insurancePersistence.test.ts src/features/rentenluecke/hooks/__tests__/lifecycleMilestonesState.test.ts`: 32 passed (22 + 8 + 2; 2 legacy persistence specs converted to `lifecycleRun`/`lifecycleValid` with full lifecycle setup).
- `GOMAXPROCS=2 npx vitest run --maxWorkers=1 src/features/rentenluecke/components/__tests__/RentenlueckeCalculator.test.tsx`: 7 passed (single authoritative lifecycle path, yearly columns, `Keine Kapitalzahl` + `kein Bypass` on capital search, validation state).
- `GOMAXPROCS=2 npx eslint .`: exit 0, clean (`lifecycleScenario` 350:7 no-useless-assignment and 523:9 unused `firstYear` clean; full run via detached `setsid nohup` under pid pressure).
- `GOMAXPROCS=2 npx tsc -b`: exit 0, clean (`lifecycleScenario` phase narrowed to `LedgerPhase`, `persistence` churchRate cast to `LifecycleTaxSettings`, test null guards for nullable capital search).
- `GOMAXPROCS=2 npx vite build`: exit 0 (chunk-size warning only, pre-existing).
- `git diff --check`: clean.

Zero-start finding (honest, no bypass): the UI proportional-scaling capital search always returns `unsupported zero-total-start` because `openingForCapital(0)` is 0 by construction (scaling today's holdings to capital 0); `allowZeroStart` stays unset (forbidden proof-bypass flag). The UI test therefore asserts `Keine Kapitalzahl` + `kein Bypass`, not a converged number. The model search test asserts explicit `zero-total-start` plus reserve `unsupported`.

## Limitations / blockers

- Container `pids.max=256` with unrelated old preview/vite servers (~191 used). Run coding/review serially; `GOMAXPROCS=2`, `npx vitest run --maxWorkers=1`. Do not kill sibling servers.
- Full jsdom suite (`RentenlueckeCalculator.test.tsx`) is timing-sensitive under pid pressure; it passed 7/7 serially via detached `setsid nohup` + `sleep 9` polls (`GOMAXPROCS=2`, `--maxWorkers=1`).
- No push/merge/deploy per task.
